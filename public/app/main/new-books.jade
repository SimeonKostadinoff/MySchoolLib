div(ng-repeat="book in books | orderBy: '-boughtDate' | limitTo: 30")
    .row
        .col-md-3
            | {{ book.boughtDate | date: 'MMM d yyyy' }}
        .col-md-9
            a(href="#/books/{{ book._id }}") {{ book.title }}
